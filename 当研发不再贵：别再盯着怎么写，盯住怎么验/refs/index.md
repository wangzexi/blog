当研发不再贵：别再盯着怎么写，盯住怎么验



中国高超音速技术领先全球的核心在于拥有以 JF-12 和 JF-22 为代表的尖端超高速风洞群，实现了高马赫数（可达30倍以上）与长实验时间（百毫秒级）的复合能力。通过“爆炸驱动”等创新技术，这些风洞能够真实复现极高空、极高速的飞行环境，使得高超音速飞行器不仅研制快，且成功率高。



核心结论

写代码正在变得极其便宜，但判定代码对不对的成本没有同步下降。

研发流程应该从"控制实现过程"转向"定义交付结果和验收标准"。

两个 AI 落地抓手：代码找得到、结果看得到。



已经发生的事

2026 年 2 月，Cloudflare 一名工程师用 AI 在 Vite 上重新实现了 Next.js 94% 的 API——路由、SSR、RSC、Server Actions、缓存、中间件，全部从零写。一个人，800 多次编程会话，一周，约 1100 美元 token 费。项目叫 vinext，已开源，带 1700 多个单测和 380 个端到端测试。

他怎么做到的：对着 Next.js 的 API 行为规范，让 AI 反复实现、测试、修正，直到行为一致。

既然 AI 只需要验收标准就能实现目标，那我们花在设计、评审上的过程时间，还有多大价值？



AI 不能一枪必中，人也不能。

但人会多开几枪。



三个前提

前提一：符号处理成本在快速下降

以 Claude 为例。24 年 3 月，Opus 3 定价每百万 Token 15/75 美元。三个月后 Sonnet 3.5 多数基准超过 Opus，价格只有20%。到 26 年初，Opus 4.6 定价 5/25 美元，Sonnet 4.6 只要 3/15 美元。

仅两年，同等能力的成本下降了一个数量级，上下文也从 200K 扩到了 1M。

各种蒸馏、量化、KV缓存、投机解码、专家网络、专有芯片也都在持续降低推理成本。



推理正在越来越快、越来越便宜。



前提二：研发的大部分产物是符号

代码、配置、接口定义、测试用例、文档、日志、SQL、页面结构——都是符号。

AI 不是只在"写代码"环节能介入，而是整条研发链路它都能介入。



前提三：研发的目标是结果，不是过程

代码不是目的，技术方案不是目的，过程文档不是目的。

目标是：系统在给定约束下，稳定产出符合预期的结果。

过程控制只是手段。



瓶颈转移：从"写"到"验"

软件本质就两件事：写和验。AI 让前者成本急剧下降。



研发成本 = 编码 + 测试



推理成本在快速下降。但工具调用——备环境、跑测试、读日志——成本没降。涉及物理世界的（硬件联调、用户实验、网络环境）更是。

数据很清楚。Faros AI 对 1 万多名开发者、1255 个团队的遥测数据：高 AI 采用率的团队任务量多 21%，PR 合并多 98%——但审查时间多 91%，PR 体积大 154%，bug 率升 9%。更要命的是，同一批团队的 DORA 指标（部署频率、交付周期、变更失败率、恢复时间）几乎没变。代码多了，交付没快。工作没消失，从"写"挪到了"验"。

Sonar 2026 全球开发者调查（1100+ 人）：96% 的开发者不完全信任 AI 代码的功能正确性，59% 认为审查 AI 代码要花"中等"或"大量"额外精力。Anthropic 自己也碰到了——工程团队代码产出过去一年涨了 200%，2026 年 3 月专门推出 Claude Code Review 来解决审查瓶颈。GitHub Octoverse 数据：约 41% 的新代码由 AI 辅助生成，月推送超 8200 万次。



过去：想法多，写出来贵现在：写出来便宜，不知道哪个是对的



有人问：AI 代码不是越来越准了吗？确实，但要看这个"准"是哪层。

AI 能写出语法正确的代码，很大程度上靠 Compiler 和 Linter。它们作为 Agent 循环里的工具，能提供毫秒级反馈——变量不存在、类型不对、导入缺失，眨眼 AI 就能改正。

但难的是第二层：业务逻辑对不对、运行行为符不符合预期、场景覆盖全不全。这些必须让代码真正跑起来，获得真实世界的反馈。



静态验证（类型、语法）已经趋近于零，动态验证（业务）依然昂贵



深一点，孤立系统的熵只会不断增大。封闭在符号世界的推理，产出的只能是“阴阳五行”一般自洽但无用的符号闭环。

如今的 Agent 的幻觉没那么明显，很大程度就是来自我们教会了它多用 Bash 和 Python 去获取负熵。

验证，是把 AI 拉回现实的锚。



实践是检验真理的唯一标准。



流程变革：从"先拍板再实现"到"先实现再筛选"



过去的生产流程是围绕“研发很贵”设计的，都是尽可能评审时间，去保护昂贵的研发时间。







但 AI 大幅降低开发成本后，人的评审、测试验收反而变最贵的环节。与其花时间争论哪个方案好，不如让 AI 先干再说。

评审只需一个：需求评审——本质就是"验收标准评审"。决定业务的输入输出是什么、要什么结果。







这里有一个关键点：验收标准应当是可自动执行的量化的标准，而不是架构、规范之类的散弹枪许愿——全看 AI 道德。



代码越来越像可再生资源，验证体系越来越像稀缺资产。



开头提到的 vinext，关键不在"AI 能写代码"，在于 AI 是对着行为规范搜索兼容实现的。没有明确的行为标准，这事做不成。真正的资产不是 Next.js 的源代码，而是它定义了"什么行为算正确"。

SQLite 代码完全开源，但核心测试框架 TH3 闭源，需要授权。核心代码 15 万行，测试代码 9200 万行——比例约 600:1，100% MC/DC 覆盖。

代码开源，测试闭源。没有完整测试套件，你没法保证数据库不丢数据。护城河不是代码，是验证体系。

2026 年 2 月，开源绘图工具 tldraw 在 GitHub 提出把测试套件（约 327 个文件）从开源仓库迁到闭源仓库（Issue #8082）。理由直接：vinext 证明一套完整测试就够 AI 从零重建整个项目，测试正在成为开源项目最需要保护的资产。提案在 Hacker News 引发大量讨论。这不是个别现象，是趋势。





两个基建：代码找得到 + 结果看得到

做 AI 落地，顺着直觉就是给 AI 写更多规范、定更多流程。但主要矛盾真的在这吗？

基建一：代码找得到

代码有最全的信息。



微服务架构下，每个服务仓库只有自己一小片的上下文。AI 在一个服务里排查问题，爬到边界就断了，然后人去找另一个服务的人，那边人再问 AI…本来能在代码里顺着调用链就能爬通的事，变成了：AI → 人 → 人沟通拉会 → 人 → AI。

建立统一仓库（monorepo）就是最简单的手段，让完整的信息对 AI 来说像本地文件一样简单、直接、可访问。

顺着这个思路，很多过程文档的价值也在变。所有的信息量，比如 SPEC 文档、设计文档、架构说明里描述的约束、数据流和交互细节，最终都会实现在代码里；代码仓库也就自然成了唯一持续更新的那份信息，代码之外的文档都会随着迭代逐渐过时不可信。

事实上，大家现在也不怎么搜文档了：大都是直接对着仓库问 AI，然后阅读 AI 面向具体问题追查代码生成的“一次性文档”。推理越便宜，越会这样用。

所以，代码仓库是唯一的 source of truth，索引和目录文档都只是它的视图，只负责指路，不承载事实本身。

基建二：结果看得到

AI 白板编程很快，但人类验证很慢。



现在的 AI 编程瓶颈不在写代码，在于写完之后：谁来跑、谁来看结果、谁来判断对不对。大多数时候是人——人启动服务，人点页面，人查日志，人看监控。AI 写完代码就停下来等，等人跑完告诉它结果，它再改，再等。一轮几秒钟的代码修改，卡在几分钟甚至几小时的人工验证上。就像工厂里，人可以停，但机器不能停——现在 AI 是那台机器，人的验证速度成了产线瓶颈。

要让 AI 自己闭环，两件事要通：第一，能跑起来——AI 能自己启动程序、触发操作、执行测试，不用等人手动操作；第二，能看到结果——日志、监控、埋点、数据库状态这些验证信息，要有机器接口（CLI、API、SQL），而不是只有给人看的 GUI 和仪表盘。

代码找得到，让 AI 有了完整的符号世界；结果看得到，让它有了闭环修正的反馈机制。两个一接通，AI 就能在符号空间里持续闭环优化——因为优化本身也是符号操作。

有些场景看起来难测——分布式故障、安全攻击、硬件交互——但本质上是同一个问题：怎么把现实世界中的行为映射到数字世界。AI 是符号世界的神，只要能把现实世界的东西建模成符号，AI 就能推理、就能闭环。这些场景不是"不能测"，是建模成本更高、验证闭环更长。



在这之后：从"造出来"到"搜出来"

一旦有了验证标准，软件研发就可以从"构造问题"变成"搜索问题"。



以前：人一步步把程序写出来。以后：人定义目标和约束，AI 在符号空间里搜索满足约束的程序。

就像做一个不定积分，正确的积分 C 取什么都对。软件也一样——通过所有测试的实现可能有好几种，每种都"正确"，但可读性、复杂度各不相同。

AI 负责找出所有满足约束的解，人根据自己的品位和工程判断，挑一个最合适的。



最终，可能就在一年后





人类下午讨论、AI 半夜干、人类上午验收反馈、AI 中午修正、人类下午再验、AI 凌晨继续干…

人类和 AI 在不同的时区，产线以半天为单位高速迭代。









附录 A：核心论断

以下可在讨论和汇报中直接引用：

"推理成本指数下降，验证成本基本不变。两条曲线反向运动，瓶颈就在这。"

"静态验证已趋近于零，动态验证依然昂贵。编译通过不等于业务正确。"

"没有真实反馈的符号推理，只会退化成自洽但没用的闭环。测试是把 AI 拉回现实的锚。"

"代码越来越像可再生资源，验证体系越来越像稀缺资产。"

"阻碍 AI 闭环的不是 AI 不够聪明，是上下文路由成本和验证系统不可编程。"

"满足约束的解不止一个，AI 负责搜索，人负责用品位挑最好的那个。"

"代码是唯一的 source of truth，文档只是它的视图。"

"Context, not control."



附录 B：两个落地抓手的实验佐证

附录 B 只做一件事：用两个很小的实验，验证正文最上面那两个落地抓手不是概念，而是可以落地的。

一、完整上下文：代码定位实验

实验环境是初步建设的索引仓库。它本身不承载业务逻辑，而是聚合 FE、BE 等多个真实子仓库，供 AI 做跨仓搜索和链路追踪。

实验任务是同一个：根据主播信息卡片需求，判断如果要在右上角增加一个“资质入口”，最可能应该改哪个文件，以及它由哪条链路触发出来。

Codex（GPT-5.4）使用的是原始图文输入：

[Image]帮我找找这个我想在主播信息卡片右上角加一个资质入口的话，应该改哪个组件

公司内模型统一跑在 Claude Code 上。在这轮实验对应的可用调用链路里，它们仅支持文本输入，因此这里使用同一份文本化 prompt：

在当前仓库搜索一个业务场景主播卡片组件，它同时满足：1）底部弹层；2）右上角有关闭按钮；3）底部有‘主播主页’和‘已关注/立即关注’按钮。现在要在这个卡片右上角新增一个‘资质入口’。请只回答三行：主改文件；触发链路文件；一句结论。

这组实验最终都收敛到了同一个主改文件 HostCard.tsx。因此表里重点比较的不是最终命中结果本身，而是模型抵达这个文件的收敛路径是否直接、是否出现错误分支、以及认知曲折度高不高。

模型

输入

结论

曲折度

收敛路径

GPT-5.4（Codex）

图文

能较稳定补齐主改文件、触发链路和修改位置

低

索引仓库 monorepo → frontend-repo → business-app/src/page/Main/Business/Header/Header.tsx → packages/Card/src/HostCard.tsx

glm-5（Claude Code）

文

已能完成主文件 + 链路定位

低

索引仓库 monorepo → packages/Card/src/HostCard.tsx → business-app/src/page/Main/Business/Header/Header.tsx

MiniMax-M2.7（Claude Code）

文

能完成主文件 + 链路定位，且链路描述更完整

低

索引仓库 monorepo → packages/Card/src/HostCard.tsx → business-app/src/page/Main/Business/Header/Header.tsx → _onClickAvatar / openHostCardModal

LongCat-Flash-Chat（Claude Code）

文

能找到主组件，但链路补全出现漂移

中

索引仓库 monorepo → packages/Card/src/HostCard.tsx → FallbackCard.tsx（错误分支）

这个实验支持两条判断：

对"产品拿图问研发改哪里"这类问题，最关键的不是预先维护一份大知识库，而是让 AI 拿到跨仓、可搜索、不断裂的完整代码上下文。

便宜模型并不是完全做不到，但它们在链路补全和稳定收敛上仍有波动。因此短期仍值得为公司内主力模型补一层导航技能，帮助它们快速剪枝搜索空间。

这层导航应被视为过渡态能力，而不是长期资产。随着未来半年模型能力提升，它应逐步拆除，避免维护成本和过时信息反过来成为新的上下文噪声。



二、验证环境和验收标准：只给验收标准的开发实验

第二个实验对应正文里的“验证环境和验收标准”。实验方式非常简单：不给实现说明，不给接口示例，只给目标和验收标准，然后观察 AI 是否能自己闭环。

可直接使用下面这段 Prompt：

目标：做一个带虚拟文件系统的 AI Agent 应用。用户可以用自然语言创建、修改文件，用户可以看到文件树和预览文件，特别是 HTML 网页。

验收标准：
- 页面分左右两块，左侧文件树显示，右侧是整个画布，画布下方有用户输入框
- 首次对话前显示引导样例（如"做一个抛物线炮弹小游戏"），点击可直接发送
- 用户发出指令后，能展示 Agent 执行进度，失败时有错误提示
- Agent创建的文件应当实时出现在文件树中，Agent 正在变更的文件应当被实时激活预览
- 点击文件树里的文件可以在右侧画布预览效果
- 页面是中文的
- 充分进行 Playwright 端到端测试，持续迭代直到所有验收点通过，也要通过视觉截图检查页面实际渲染和交互效果

技术建议：
- 技术选型参考开发社区主流方案，自行搜索确定
- 调用公司内部大模型平台 统一模型平台 统一对话接口进行推理

参考资料：
- [调用统一模型平台（Node.js、纯前端JavaScript）](#)

这个实验的重点不是"AI 能不能写页面"，而是：

人没有提供实现过程控制，只提供目标和验收标准。

AI 会自己搜索互联网补齐上下文、自己写代码、自己跑测试、自己修正。

真正让它闭环的，不是规范，不是设计说明，而是可执行、可自动化、可 pass/fail 的验证系统。

而且这个实验产出的东西本身就是一个微型 Agent——它自己就在做"推理 → 工具调用 → 观察结果 → 继续"的循环，跟正文描述的范式转移形成了呼应：AI 不只是在对话，而是在通过工具操作一个环境、产出可验证的交付物。

这正对应正文里的第二个抓手：当实现成本快速下降后，真正稀缺的不是研发能力，而是让 AI 可调用、可反复执行、可自动判对错的验证系统。



关键：本文基于 2026 年 3 月技术现状撰写。